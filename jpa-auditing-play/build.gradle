/**
 * 다음 커맨드를 사용해서 실행 가능한 jar 파일을 생성할 수 있다.
 * ./gradlew clean bootJar
 */
bootJar {
    mainClassName = 'smart.kuk.KukApplication'
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
}
